export type TeamList = {
  name: string;
  points: number;
  team_id: number;
  tournament_id: number;
};

export type TeamListData = {
  value: number;
  label: string;
  name: string;
  points: number;
  team_id: number;
  tournament_id: number;
};
